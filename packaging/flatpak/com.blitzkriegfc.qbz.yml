app-id: com.blitzkriegfc.qbz
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
command: qbz
finish-args:
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --device=dri
  - --filesystem=xdg-download
  - --filesystem=xdg-music
  - --filesystem=xdg-cache
  - --filesystem=xdg-config
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=org.freedesktop.portal.Desktop
  - --talk-name=org.freedesktop.portal.FileChooser

modules:
  - shared-modules/libayatana-appindicator/libayatana-appindicator-gtk3.json
  - name: qbz
    buildsystem: simple
    build-options:
      env:
        CARGO_HOME: /run/build/qbz/.cargo
        RUSTUP_HOME: /run/build/qbz/.rustup
        npm_config_cache: /run/build/qbz/.npm
        PATH: /run/build/qbz/node/bin:/run/build/qbz/.cargo/bin:/usr/bin:/bin
      build-args:
        - --share=network
    build-commands:
      # Install Node.js 20 LTS from tarball
      - curl -fsSL https://nodejs.org/dist/v20.19.2/node-v20.19.2-linux-x64.tar.xz | tar -xJ
      - mv node-v20.19.2-linux-x64 node
      # Install Rust via rustup
      - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable --profile minimal
      # Build the application
      - npm ci
      - npm run build
      - cd src-tauri && cargo build --release
      # Install the binary and assets
      - install -Dm755 src-tauri/target/release/qbz-nix /app/bin/qbz
      - install -Dm644 packaging/flatpak/com.blitzkriegfc.qbz.desktop /app/share/applications/com.blitzkriegfc.qbz.desktop
      - install -Dm644 packaging/flatpak/com.blitzkriegfc.qbz.metainfo.xml /app/share/metainfo/com.blitzkriegfc.qbz.metainfo.xml
      - install -Dm644 src-tauri/icons/128x128.png /app/share/icons/hicolor/128x128/apps/com.blitzkriegfc.qbz.png
    sources:
      - type: dir
        path: ../..
        skip:
          - .git
          - node_modules
          - src-tauri/target
          - .svelte-kit
